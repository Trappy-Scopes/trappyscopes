# Tier 0 -- the bare minimum to reach and render `trappyscope --launcher`'s
# menu, nothing more. pip only, on purpose: this has to install correctly
# on a completely fresh machine before anything else (uv included) exists.
#
# Verified empirically, not guessed: traced sys.modules while importing
# core.argparser + launcher.tui and actually rendering the menu (version
# line, venv line). Every third-party package pulled in that way is listed
# below; nothing else. GitPython's own dependencies (gitdb, smmap) are not
# listed here -- pip resolves those automatically.
#
# Version pins match pyproject.toml's for the same packages, so this file
# and the main dependency list never quietly drift apart on what "colorama"
# means.

colorama>=0.4.6,<0.5.0
pyyaml==6.0.1
confuse>=2.0.1,<3.0.0
gitpython>=3.1.45,<4.0.0
rich>=14.1.0,<15.0.0
